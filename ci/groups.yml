groups:
  - name: master
    jobs:
      - management-dev
      - management
  - name: pull-request
    jobs:
      - blackbox-exporter-pr
  - name: mirror
    jobs:
      - mirror-dwpdigital-blackbox-exporter-dev
      - mirror-dwpdigital-blackbox-exporter
